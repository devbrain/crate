#include <crate/crate.hh>

// This file can be used for library-wide initialization or
// convenience functions in the future.
//
// VFS functionality has been removed from crate.
// Use ares VFS for filesystem abstractions with crate archives as backends.

namespace crate {
    // Currently empty - version info is constexpr in header
} // namespace crate
